[build]
  command = "npm run build"
  publish = "dist"

[dev]
  command = "npm run serve"
  port = 8080

# Redirects para SPAs (evita 404 en rutas internas)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
